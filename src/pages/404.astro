---
import Layout from "../layouts/Layout.astro";

// Captura el parámetro `id` desde la URL
const { id } = Astro.params || {};
console.log("ID:", id); // Esto te ayudará a depurar y confirmar si el ID está siendo capturado.
---

<Layout title="Vehículo no encontrado" description="Vehículo no encontrado">
  <main class="flex flex-col items-center justify-center min-h-screen bg-gray-50 text-center px-4">
    <div class="max-w-3xl bg-white shadow-md rounded-lg p-8 mt-10 border border-gray-200">
      <h1 class="text-4xl font-extrabold text-gray-800 mb-4">404 - Vehículo no encontrado</h1>
      <p class="text-lg text-gray-600 mb-6">
        Lamentamos las molestias, no pudimos encontrar un vehículo.
      </p>
      <a 
        href="/catalogo" 
        class="inline-block px-6 py-3 text-white bg-blue-600 hover:bg-blue-700 rounded-lg shadow-md transition"
      >
        Volver al catálogo
      </a>
    </div>
  </main>
</Layout>
